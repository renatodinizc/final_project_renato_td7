<h1> Bem vindo ao FreelancingHUB </h1>

<% if @projects.empty? %>
    <p> Ainda não existem projetos cadastrados </p>
<% else %>
    <% @projects.each do |project| %>
        <p>
        <%= link_to project.title, project %>
        <br />
        <%= project.description %>
        <br />
        <%= project.desired_skills %>
        <br />
        </p>
    <% end %>
<% end %>

